# PostgreSQL repository assets

This directory reserves the SQL-first repository layout for Milestone 2. No schema,
migration, seed, function, view, or test data is implemented during the Milestone 0
and Milestone 1 scaffolding assignment.

Future migrations will be immutable, numbered, checksummed, UTC-only, and applied by
an explicit migration runner. Automatic ORM schema generation is out of scope.
