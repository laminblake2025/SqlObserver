[CmdletBinding()]
param()

Set-StrictMode -Version Latest
$ErrorActionPreference = 'Stop'

Write-Host 'SqlObserver lab seeding is intentionally unavailable until repository migrations and ingestion exist.'
Write-Host 'No target SQL Server or PostgreSQL repository was modified.'
