[CmdletBinding()]
param()

Set-StrictMode -Version Latest
$ErrorActionPreference = 'Stop'

Write-Host 'SqlObserver runtime startup is intentionally unavailable in Milestone 1.'
Write-Host 'The PostgreSQL development topology and service hosts will be added in dependency order in later milestones.'
